name: PR check

on:
  pull_request:
    branches: [ master ]

jobs:
  engine-unittest:
    needs: engine-lint
    uses: hanshuaikang/bamboo-engine/.github/workflows/engine_unittest.yml@feature/release_pipeline_3_25_2_fix_codecov
